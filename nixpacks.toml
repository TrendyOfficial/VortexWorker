[variables]
NODE_ENV = "production"
PORT = "8787"

[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm run typecheck", "pnpm run build"]

[start]
cmd = "node dist/server.js"
